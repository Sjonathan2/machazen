# Database (PostgreSQL - pakai Vercel Postgres untuk production)
# Untuk local development, comment line di atas dan uncomment yang lokal
DATABASE_URL="postgresql://user:password@host:5432/machazen"

# JWT Secret (ganti dengan string acak untuk production)
JWT_SECRET="dev_secret_change_in_prod"
